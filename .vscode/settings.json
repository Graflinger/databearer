{
    "sqltools.connections": [
        {
            "previewLimit": 200,
            "driver": "DuckDB",
            "name": "duckdb",
            "database": "${workspaceFolder:databearer}/.data/duckdb.db"
        }
    ],
    "sqltools.useNodeRuntime": true,
    "dbt.enableNewLineagePanel": true
}
